rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Browsers never access Firestore directly. The Cloud Run service uses ADC,
    // and Admin SDK requests are authorized with IAM rather than these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
